export{}

/* import React from 'react'

function UpdateProducts() {
	const {user , setUser, update} = React.useContext(usercontext) as Iusercontext


		useEffect (() => { 3:58 h // useParams
			
			
			
		})

	return (
		
		<form onSubmit = {update} classname"setName">
		
			<h1>set a header </h1>
			<inpud type='hiddenn' value{user.id}
			<input value {user.firstname} onchange= {(e) => setUser ({-...setUser, firstname: e.target.value})} 
			<input value {user.lastname} onchange= {(e) => setUser ({-...setUser, lastname: e.target.value})} 
			<input value {user.email} onchange= {(e) => setUser ({-...setUser, email: e.target.value})} 
			
			<button type submit>
			
		</form>
	)

}

export default UpdateProducts */